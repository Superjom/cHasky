#include "layers/NNProjection.h"

template <typename T>
void NNProjection<T>::forward   (Argument& fromag, Argument& toag) {
    cout << "hello world";
}

template <typename T>
void NNProjection<T>::backward   (Argument& fromag, Argument& toag) {
    cout << "hello world";
}
